LINE GIRL - แพลตฟอร์มสั่งอาหารออนไลน์
LINE GIRL คือเว็บแอปพลิเคชันสำหรับสั่งอาหารออนไลน์เต็มรูปแบบ ที่พัฒนาด้วย Next.js และ React 19 โดยออกแบบมาเพื่อเชื่อมต่อผู้ใช้งาน 3 กลุ่มหลักเข้าด้วยกัน:

Customer (ลูกค้า): ผู้ใช้งานทั่วไปสำหรับค้นหาและสั่งซื้ออาหาร

Shop (ร้านค้า): เจ้าของร้านอาหารสำหรับจัดการร้านค้าและเมนูของตนเอง

Admin (ผู้ดูแลระบบ): ผู้ดูแลระบบสำหรับจัดการข้อมูลผู้ใช้ทั้งหมด

🚀 ฟีเจอร์หลัก (Key Features)
โปรเจกต์นี้มีระบบการทำงานที่แบ่งตามบทบาทของผู้ใช้อย่างชัดเจน:

👤 สำหรับลูกค้า (Customer)
ระบบสมาชิก: สมัครสมาชิกและล็อกอินสำหรับลูกค้า

ค้นหาร้านค้า: ค้นหาและฟิลเตอร์ร้านอาหารตามประเภท (Category)

ดูเมนู: ดูหน้าร้านและเมนูของแต่ละร้านค้า/page.jsx`]

ระบบตะกร้าสินค้า: เพิ่ม/ลด/ลบ สินค้าลงในตะกร้า (จัดการผ่าน React Context)

การสั่งซื้อ: ยืนยันคำสั่งซื้อและเลือกวิธีชำระเงิน

ระบบรีวิว: สามารถเขียนรีวิวและให้คะแนน (Rating) แยกตามเมนูสินค้าได้ หลังจาก สั่งซื้อสำเร็จเท่านั้น

🏪 สำหรับร้านค้า (Shop)
ระบบสมาชิก: สมัครสมาชิกและล็อกอินสำหรับร้านค้า

Onboarding: ร้านค้าที่ล็อกอินครั้งแรก (ยังไม่มีร้าน) จะถูกบังคับไปหน้าสร้างร้านค้า (/manage/create) ก่อน

จัดการร้าน: สร้างและอัปโหลดรูปภาพโปรไฟล์ร้านค้า

จัดการเมนู (CRUD): เพิ่ม, ลบ, แก้ไข และดูเมนูทั้งหมดของร้านตัวเอง/page.jsx`]

จัดการไฟล์: ระบบรองรับการอัปโหลดรูปภาพเมนู และจะลบรูปภาพออกจากเซิร์ฟเวอร์อัตโนมัติเมื่อเมนูถูกลบ/route.js`]

🔐 สำหรับผู้ดูแลระบบ (Admin)
ระบบสมาชิก: ล็อกอินสำหรับ Admin

จัดการผู้ใช้ (CRUD): ดู, สร้าง, แก้ไข และลบผู้ใช้ทั้งหมดในระบบ (ทั้ง Customer, Shop, Admin)/route.js`]

การป้องกัน: Admin ไม่สามารถลบบัญชีของตัวเองได้ และมีระบบป้องกันการลบผู้ใช้ที่ยังมีข้อมูล (เช่น ร้านค้า, ออเดอร์) ค้างอยู่/route.js`]

🛠️ เทคโนโลยีที่ใช้ (Tech Stack)
Frontend:

React 19

Next.js 15 (App Router)

Tailwind CSS

Flowbite React (สำหรับ UI Components)

Backend:

Next.js API Routes

Database:

MySQL (เชื่อมต่อผ่าน mysql2)

Authentication:

JWT (JSON Web Token)

HTTP-only Cookies (เพื่อความปลอดภัย)

Bcrypt (สำหรับ Hashing รหัสผ่าน)

💿 การติดตั้งและเริ่มต้นใช้งาน (Getting Started)
Clone โปรเจกต์:

Bash

git clone [Your Repository URL]
cd mypro1
ติดตั้ง Dependencies:

Bash

npm install
(โปรเจกต์นี้ใช้ package.json)

ตั้งค่าฐานข้อมูล:

Import สคริปต์ .sql ของคุณลงใน MySQL Database

ตั้งค่า Environment Variables:

สร้างไฟล์ .env.local ที่ Root ของโปรเจกต์

เพิ่มค่าที่จำเป็นสำหรับเชื่อมต่อฐานข้อมูลและ JWT Secret

ข้อมูลโค้ด

# .env.local
DB_HOST=localhost
DB_USER=[Your_DB_User]
DB_PASSWORD=[Your_DB_Password]
DB_DATABASE=[Your_DB_Name]

JWT_SECRET=[Your_Very_Secret_Key]
รันเซิร์ฟเวอร์ (Development Mode):

Bash

npm run dev
(จาก package.json)

เปิดเว็บ:

เข้าที่ http://localhost:3000